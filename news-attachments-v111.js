(()=>{
"use strict";
if(window.__NEWS_ATTACHMENTS_V111__)return;window.__NEWS_ATTACHMENTS_V111__=1;
const OWNER="Sunarso29",REPO="paibp-smart",BRANCH="main";
const GH=`https://api.github.com/repos/${OWNER}/${REPO}/contents/`;
const WORKER="https://paibp-smart-api.sunarso29.workers.dev";
const KEY="b082937b2165453ba7d9f81ecac063b00310b339ec0643da";
const TOKEN_KEY="paibp-smart-media-github-token-session-v110";
const MAX_VIDEOS=5,MAX_BYTES=95*1024*1024;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const uid=()=>crypto?.randomUUID?.()||`att-${Date.now()}-${Math.random().toString(36).slice(2,9)}`;
const extOf=n=>{const a=String(n||"").split(".");return a.length>1?a.pop().toLowerCase().replace(/[^a-z0-9]+/g,""):"bin"};
const slug=s=>String(s||"media").normalize("NFKD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9._-]+/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,"").slice(0,70)||"media";
const bytes=n=>{n=+n||0;if(n<1024)return`${n} B`;if(n<1048576)return`${(n/1024).toFixed(1)} KB`;return`${(n/1048576).toFixed(1)} MB`};
let videoQueue=[],audioQueue=[],remote=[],loadingId="",bypassSubmit=false,dirty=false;

function headers(token=""){const h={Accept:"application/vnd.github+json","X-GitHub-Api-Version":"2022-11-28"};if(token)h.Authorization=`Bearer ${token}`;return h}
async function ghWrite(path,body,token){const url=GH+path.split("/").map(encodeURIComponent).join("/");const r=await fetch(url,{method:"PUT",headers:{...headers(token),"Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok){let m=`GitHub HTTP ${r.status}`;try{const j=await r.json();m=j.message||m}catch{}throw new Error(m)}return r.json()}
function fileB64(file){return new Promise((res,rej)=>{const fr=new FileReader();fr.onload=()=>{const s=String(fr.result||"");res(s.slice(s.indexOf(",")+1))};fr.onerror=()=>rej(fr.error||new Error("File gagal dibaca"));fr.readAsDataURL(file)})}
async function workerPost(action,data){const r=await fetch(WORKER,{method:"POST",cache:"no-store",headers:{"Content-Type":"text/plain;charset=UTF-8","Accept":"application/json"},body:JSON.stringify({action,readKey:KEY,key:KEY,data:{...data,readKey:KEY},origin:location.origin})});const t=await r.text();let j={};try{j=JSON.parse(t)}catch{}if(!r.ok||j?.ok===false)throw new Error(j?.error||`Server HTTP ${r.status}`);return j}
const unwrap=v=>v&&typeof v==="object"&&Object.prototype.hasOwnProperty.call(v,"value")?v.value:v;
async function snapshot(){const u=new URL(WORKER);u.searchParams.set("action","publicSnapshot");u.searchParams.set("_",Date.now());const r=await fetch(u,{cache:"no-store"});if(!r.ok)throw new Error("Lampiran belum dapat dibaca.");return r.json()}
function token(){return sessionStorage.getItem(TOKEN_KEY)||""}
function status(m,t=""){const e=$("#nea-status");if(e){e.textContent=m||"";e.dataset.tone=t}}
function progress(v){const e=$("#nea-progress span");if(e)e.style.width=`${Math.max(0,Math.min(100,+v||0))}%`}
function newsId(){let id=$("#ne-id")?.value||"";if(!id){id=`news-${Date.now()}-${Math.random().toString(36).slice(2,9)}`;if($("#ne-id"))$("#ne-id").value=id}return id}
function totalVideos(){return remote.filter(x=>x.type==="video").length+videoQueue.length}

function add(kind,files){let added=0;for(const f of [...files]){if(f.size>MAX_BYTES){status(`“${f.name}” melebihi 95 MB. Pilih file yang lebih kecil agar unggahan dari HP tetap stabil.`,"error");continue}if(kind==="video"&&totalVideos()>=MAX_VIDEOS){status("Maksimal 5 video untuk satu posting.","error");break}(kind==="video"?videoQueue:audioQueue).push({id:uid(),file:f,type:kind});added++}if(added)dirty=true;render();if(added)status(`${added} ${kind==="video"?"video":"audio"} ditambahkan ke posting. File baru dikirim saat tombol Terbitkan ditekan.`)}
function render(){const v=$("#nea-video-count"),a=$("#nea-audio-count");if(v)v.textContent=`${totalVideos()}/${MAX_VIDEOS} video`;if(a)a.textContent=`${remote.filter(x=>x.type==="audio").length+audioQueue.length} audio`;const root=$("#nea-list");if(!root)return;const queued=[...videoQueue,...audioQueue],all=[...remote.map(x=>({...x,remote:true})),...queued.map(x=>({id:x.id,type:x.type,name:x.file.name,size:x.file.size,queued:true}))];if(!all.length){root.innerHTML="";return}root.innerHTML=all.map(x=>`<div class="nea-item" data-type="${x.type}"><span class="nea-icon">${x.type==="video"?"▶":"♫"}</span><span><strong>${esc(x.name||x.title||"Media")}</strong><small>${x.remote?"Tersimpan pada posting":`Siap diunggah • ${bytes(x.size)}`}</small></span><button class="nea-remove" type="button" data-nea-remove="${esc(x.id)}" data-remote="${x.remote?1:0}">${x.remote?"Lepas dari posting":"Hapus"}</button></div>`).join("");$$('[data-nea-remove]',root).forEach(b=>b.onclick=()=>{dirty=true;if(b.dataset.remote==="1"){remote=remote.filter(x=>String(x.id)!==String(b.dataset.neaRemove));status("Lampiran dilepas dari posting. Perubahan disimpan saat posting diterbitkan lagi.")}else{videoQueue=videoQueue.filter(x=>String(x.id)!==String(b.dataset.neaRemove));audioQueue=audioQueue.filter(x=>String(x.id)!==String(b.dataset.neaRemove))}render()})}

async function loadRemote(id){if(!id||id===loadingId)return;loadingId=id;try{const s=await snapshot(),c=s?.content||{},x=unwrap(c[`news:${id}:attachments`])||{};remote=Array.isArray(x.items)?x.items:[]}catch{remote=[]}finally{dirty=false;render()}}

async function uploadOne(q,id,index,total){const t=token();if(!t)throw new Error("Masukkan GitHub token pada bagian Video & Audio. Token hanya disimpan selama tab editor terbuka.");const f=q.file,ext=extOf(f.name),name=slug(f.name.replace(/\.[^.]+$/,"")||f.name),path=`news-media/${id}/${q.type}/${Date.now()}-${index}-${name}.${ext}`;status(`Mengunggah ${q.type==="video"?"video":"audio"} ${index}/${total}: ${f.name}`);progress(Math.max(4,Math.round((index-1)/total*88)));const content=await fileB64(f);const out=await ghWrite(path,{message:`Lampiran ${q.type} berita ${id}`,content,branch:BRANCH},t);return{id:q.id,type:q.type,name:f.name,title:f.name.replace(/\.[^.]+$/,"")||f.name,path,url:out?.content?.download_url||`https://raw.githubusercontent.com/${OWNER}/${REPO}/${BRANCH}/${path.split("/").map(encodeURIComponent).join("/")}`,size:f.size,mime:f.type||"",ext,createdAt:new Date().toISOString()}}

async function saveAttachmentManifest(id){const value={schema:"news-attachments-v111",newsId:id,updatedAt:new Date().toISOString(),items:remote.map((x,i)=>({...x,order:i+1}))};await workerPost("contentUpsert",{key:`news:${id}:attachments`,value,authorName:"Sunarso, S.Pd.I, Gr",authorSchool:"SMP Negeri 1 Susukan",updatedAt:new Date().toISOString()})}

async function prepareAndContinue(){const id=newsId(),q=[...videoQueue,...audioQueue];if(totalVideos()>MAX_VIDEOS){status("Maksimal 5 video untuk satu posting.","error");return}if(q.length&&!token()){status("Untuk mengirim video/audio, isi GitHub token sekali pada bagian Video & Audio. Token tidak disimpan permanen.","error");$("#nea-token")?.focus();return}const submit=$("#ne-publish-direct");if(submit){submit.disabled=true;submit.textContent="Mengunggah media…"}try{for(let i=0;i<q.length;i++){const item=await uploadOne(q[i],id,i+1,q.length);remote.push(item)}videoQueue=[];audioQueue=[];status("Menyimpan hubungan media dengan posting…");progress(94);await saveAttachmentManifest(id);dirty=false;progress(100);render();status("Video/audio siap. Melanjutkan penerbitan berita…","ok");bypassSubmit=true;requestAnimationFrame(()=>{$("#ne-form")?.dispatchEvent(new Event("submit",{bubbles:true,cancelable:true}))})}catch(e){status(`Media belum terkirim: ${e.message||e}`,"error");if(submit){submit.disabled=false;submit.textContent="Terbitkan"}progress(0)}}

function mount(){const form=$("#ne-form");if(!form||$("#nea-section"))return;const actions=$(".ne-actions",form);const sec=document.createElement("section");sec.className="ne-form-section nea-section";sec.id="nea-section";sec.innerHTML=`<div class="ne-form-section-head nea-head"><div><span class="ne-section-kicker">04 • VIDEO & AUDIO</span><strong>Lampiran multimedia posting</strong><small>Video dan audio tampil di posting berita yang sama, bukan sebagai fitur terpisah.</small></div><div class="nea-counts"><span class="nea-badge" id="nea-video-count">0/5 video</span><span class="nea-badge audio" id="nea-audio-count">0 audio</span></div></div><div class="nea-upload-grid"><label class="nea-picker"><input id="nea-video" type="file" multiple><span><b>＋ Unggah video</b><small>Maksimal 5 video per posting • semua ekstensi dapat dipilih</small></span></label><label class="nea-picker audio"><input id="nea-audio" type="file" multiple><span><b>＋ Unggah MP3 / audio</b><small>Jumlah audio tidak dibatasi editor • semua ekstensi dapat dipilih</small></span></label></div><div class="nea-token"><input id="nea-token" type="password" autocomplete="off" placeholder="GitHub token untuk menyimpan file media"><button id="nea-token-save" type="button">Gunakan di tab ini</button><p class="nea-token-note">Token hanya berada di session browser dan tidak ditulis ke posting atau repository. Jika tidak menambahkan video/audio, bagian ini dapat diabaikan.</p></div><div class="nea-list" id="nea-list"></div><div class="nea-progress" id="nea-progress"><span></span></div><div class="nea-status" id="nea-status" aria-live="polite"></div><p class="nea-note"><b>Ringan di HP:</b> media tidak diputar dan tidak diunduh saat halaman berita dibuka. Sumber video/audio baru dipasang setelah pengunjung menekan Putar. Dukungan pemutaran tetap mengikuti codec yang tersedia di browser/perangkat.</p>`;actions?.before(sec);$("#nea-video").onchange=e=>{add("video",e.target.files||[]);e.target.value=""};$("#nea-audio").onchange=e=>{add("audio",e.target.files||[]);e.target.value=""};$("#nea-token-save").onclick=()=>{const v=$("#nea-token").value.trim();if(!v){sessionStorage.removeItem(TOKEN_KEY);status("Token sesi dikosongkan.");return}sessionStorage.setItem(TOKEN_KEY,v);$("#nea-token").value="";status("Token aktif untuk tab editor ini.","ok")};render()}

mount();
document.addEventListener("submit",e=>{if(e.target?.id!=="ne-form")return;if(bypassSubmit){bypassSubmit=false;return}if(!videoQueue.length&&!audioQueue.length&&!remote.length&&!dirty)return;e.preventDefault();e.stopImmediatePropagation();prepareAndContinue()},{capture:true});
document.addEventListener("click",e=>{const edit=e.target.closest?.("[data-edit]");if(edit)setTimeout(()=>loadRemote($("#ne-id")?.value||edit.dataset.edit),80);if(e.target.closest?.("#ne-reset")){videoQueue=[];audioQueue=[];remote=[];loadingId="";dirty=false;render();status("");progress(0)}},true);
const initial=$("#ne-id")?.value;if(initial)loadRemote(initial);
})();
