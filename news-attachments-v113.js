(()=>{
"use strict";
if(window.__NEWS_ATTACHMENTS_V113__)return;window.__NEWS_ATTACHMENTS_V113__=1;
const WORKER="https://paibp-smart-api.sunarso29.workers.dev";
const KEY="b082937b2165453ba7d9f81ecac063b00310b339ec0643da";
const CFG=window.PAIBP_CLOUDINARY_MEDIA||{};
const MAX_VIDEOS=Number(CFG.maxVideos||5);
const MAX_VIDEO_BYTES=Number(CFG.maxVideoBytes||100*1024*1024);
const MAX_AUDIO_BYTES=Number(CFG.maxAudioBytes||25*1024*1024);
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const uid=()=>crypto?.randomUUID?.()||`att-${Date.now()}-${Math.random().toString(36).slice(2,9)}`;
const bytes=n=>{n=+n||0;if(n<1024)return`${n} B`;if(n<1048576)return`${(n/1024).toFixed(1)} KB`;return`${(n/1048576).toFixed(1)} MB`};
let videoQueue=[],audioQueue=[],remote=[],loadingId="",bypassSubmit=false,dirty=false;

function getCloudName(){return String(localStorage.getItem("paibpCloudName")||CFG.cloudName||"").trim()}
function getPreset(){return String(localStorage.getItem("paibpCloudinaryPreset")||CFG.uploadPreset||"").trim()}
function configured(){return Boolean(getCloudName()&&getPreset())}
function storeConfig(){const c=$("#nea-cloud-name")?.value.trim()||"",p=$("#nea-upload-preset")?.value.trim()||"";if(c)localStorage.setItem("paibpCloudName",c);if(p)localStorage.setItem("paibpCloudinaryPreset",p);renderSetup();status(configured()?"Konfigurasi Cloudinary tersimpan di perangkat ini. Tidak ada token/kartu yang digunakan.":"Lengkapi Cloud name dan unsigned upload preset.",configured()?"ok":"error")}
function renderSetup(){const box=$("#nea-cloud-setup"),ok=$("#nea-cloud-ready");if(!box)return;const ready=configured();box.hidden=ready;if(ok){ok.hidden=!ready;ok.textContent=ready?`Cloudinary gratis aktif • ${getCloudName()}`:""}}

async function workerPost(action,data){
  const r=await fetch(WORKER,{method:"POST",cache:"no-store",headers:{"Content-Type":"text/plain;charset=UTF-8","Accept":"application/json"},body:JSON.stringify({action,readKey:KEY,key:KEY,data:{...data,readKey:KEY},origin:location.origin})});
  const t=await r.text();let j={};try{j=JSON.parse(t)}catch{}
  if(!r.ok||j?.ok===false)throw new Error(j?.error||`Server HTTP ${r.status}`);return j
}
const unwrap=v=>v&&typeof v==="object"&&Object.prototype.hasOwnProperty.call(v,"value")?v.value:v;
async function snapshot(){const u=new URL(WORKER);u.searchParams.set("action","publicSnapshot");u.searchParams.set("_",Date.now());const r=await fetch(u,{cache:"no-store"});if(!r.ok)throw new Error("Lampiran belum dapat dibaca.");return r.json()}
function status(m,t=""){const e=$("#nea-status");if(e){e.textContent=m||"";e.dataset.tone=t}}
function progress(v){const e=$("#nea-progress span");if(e)e.style.width=`${Math.max(0,Math.min(100,+v||0))}%`}
function newsId(){let id=$("#ne-id")?.value||"";if(!id){id=`news-${Date.now()}-${Math.random().toString(36).slice(2,9)}`;if($("#ne-id"))$("#ne-id").value=id}return id}
function totalVideos(){return remote.filter(x=>x.type==="video").length+videoQueue.length}
function classifyFile(file,kind){const mime=String(file.type||"").toLowerCase(),name=String(file.name||"").toLowerCase();if(kind==="video")return mime.startsWith("video/")||/\.(mp4|webm|mov|m4v|avi|mkv|3gp|3g2|ogv|mpeg|mpg|mts|m2ts|wmv|flv)$/i.test(name);return mime.startsWith("audio/")||/\.(mp3|wav|m4a|aac|ogg|oga|opus|flac|weba|aiff|aif|amr|wma)$/i.test(name)}
function add(kind,files){let added=0;for(const f of [...files]){if(!classifyFile(f,kind)){status(`“${f.name}” tidak dikenali sebagai ${kind==="video"?"video":"audio"}.`,"error");continue}const limit=kind==="video"?MAX_VIDEO_BYTES:MAX_AUDIO_BYTES;if(f.size>limit){status(`“${f.name}” melebihi batas ${kind==="video"?"100 MB":"25 MB"} pada penyimpanan gratis.`,"error");continue}if(kind==="video"&&totalVideos()>=MAX_VIDEOS){status("Maksimal 5 video untuk satu posting.","error");break}(kind==="video"?videoQueue:audioQueue).push({id:uid(),file:f,type:kind});added++}if(added)dirty=true;render();if(added)status(`${added} ${kind==="video"?"video":"audio"} ditambahkan. Media baru dikirim saat tombol Terbitkan ditekan.`)}
function render(){const v=$("#nea-video-count"),a=$("#nea-audio-count");if(v)v.textContent=`${totalVideos()}/${MAX_VIDEOS} video`;if(a)a.textContent=`${remote.filter(x=>x.type==="audio").length+audioQueue.length} audio`;const root=$("#nea-list");if(!root)return;const queued=[...videoQueue,...audioQueue],all=[...remote.map(x=>({...x,remote:true})),...queued.map(x=>({id:x.id,type:x.type,name:x.file.name,size:x.file.size,queued:true}))];root.innerHTML=all.map(x=>`<div class="nea-item" data-type="${x.type}"><span class="nea-icon">${x.type==="video"?"▶":"♫"}</span><span><strong>${esc(x.name||x.title||"Media")}</strong><small>${x.remote?"Tersimpan pada posting":`Siap dikirim • ${bytes(x.size)}`}</small></span><button class="nea-remove" type="button" data-nea-remove="${esc(x.id)}" data-remote="${x.remote?1:0}">${x.remote?"Lepas dari posting":"Hapus"}</button></div>`).join("");$$('[data-nea-remove]',root).forEach(b=>b.onclick=()=>{dirty=true;if(b.dataset.remote==="1")remote=remote.filter(x=>String(x.id)!==String(b.dataset.neaRemove));else{videoQueue=videoQueue.filter(x=>String(x.id)!==String(b.dataset.neaRemove));audioQueue=audioQueue.filter(x=>String(x.id)!==String(b.dataset.neaRemove))}render()})}
async function loadRemote(id){if(!id||id===loadingId)return;loadingId=id;try{const s=await snapshot(),c=s?.content||{},x=unwrap(c[`news:${id}:attachments`])||{};remote=Array.isArray(x.items)?x.items:[]}catch{remote=[]}finally{dirty=false;render()}}

async function uploadOne(q,id,index,total){
  if(!configured())throw new Error("Cloudinary belum dikonfigurasi. Isi Cloud name dan unsigned upload preset sekali saja.");
  const cloud=getCloudName(),preset=getPreset(),f=q.file,fd=new FormData();
  fd.append("file",f,f.name);fd.append("upload_preset",preset);fd.append("folder",`${CFG.folder||"paibp-smart/news"}/${id}`);fd.append("tags",`paibp-smart,news,${q.type}`);fd.append("context",`news_id=${id}|attachment_id=${q.id}|kind=${q.type}`);
  status(`Mengunggah ${q.type==="video"?"video":"audio"} ${index}/${total}: ${f.name}`);progress(Math.max(4,Math.round((index-1)/Math.max(total,1)*88)));
  const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),120000);
  try{
    const r=await fetch(`https://api.cloudinary.com/v1_1/${encodeURIComponent(cloud)}/video/upload`,{method:"POST",body:fd,signal:ctl.signal});const t=await r.text();let j={};try{j=JSON.parse(t)}catch{}
    if(!r.ok||!j?.secure_url)throw new Error(j?.error?.message||`Cloudinary HTTP ${r.status}`);
    return{id:q.id,type:q.type,name:f.name,title:f.name.replace(/\.[^.]+$/,"")||f.name,size:Number(j.bytes||f.size||0),mime:f.type||"",url:j.secure_url,publicId:j.public_id||"",resourceType:j.resource_type||"video",format:j.format||"",duration:Number(j.duration||0),width:Number(j.width||0),height:Number(j.height||0),createdAt:new Date().toISOString(),storage:"cloudinary-free"}
  }catch(e){if(e?.name==="AbortError")throw new Error("Unggahan melewati batas waktu. Coba file lebih kecil atau koneksi yang lebih stabil.");throw e}finally{clearTimeout(timer)}
}
async function saveAttachmentManifest(id){const value={schema:"news-attachments-v113",newsId:id,updatedAt:new Date().toISOString(),storage:"cloudinary-free",items:remote.map((x,i)=>({...x,order:i+1}))};await workerPost("contentUpsert",{key:`news:${id}:attachments`,value,authorName:"Sunarso, S.Pd.I, Gr",authorSchool:"SMP Negeri 1 Susukan",updatedAt:new Date().toISOString()})}
async function prepareAndContinue(){const id=newsId(),q=[...videoQueue,...audioQueue];if(totalVideos()>MAX_VIDEOS){status("Maksimal 5 video untuk satu posting.","error");return}if(q.length&&!configured()){renderSetup();status("Isi Cloud name dan unsigned upload preset sekali saja. Tidak perlu kartu, token GitHub, atau API secret.","error");$("#nea-cloud-setup")?.scrollIntoView({behavior:"smooth",block:"center"});return}const submit=$("#ne-publish-direct");if(submit){submit.disabled=true;submit.textContent="Mengunggah media…"}try{for(let i=0;i<q.length;i++){const item=await uploadOne(q[i],id,i+1,q.length);remote.push(item)}videoQueue=[];audioQueue=[];status("Menyimpan hubungan media dengan posting…");progress(94);await saveAttachmentManifest(id);dirty=false;progress(100);render();status("Video/audio siap. Melanjutkan penerbitan berita…","ok");bypassSubmit=true;requestAnimationFrame(()=>{$("#ne-form")?.dispatchEvent(new Event("submit",{bubbles:true,cancelable:true}))})}catch(e){status(`Media belum terkirim: ${e.message||e}`,"error");if(submit){submit.disabled=false;submit.textContent="Terbitkan"}progress(0)}}

function mount(){const form=$("#ne-form");if(!form||$("#nea-section"))return;const actions=$(".ne-actions",form);const sec=document.createElement("section");sec.className="ne-form-section nea-section";sec.id="nea-section";sec.innerHTML=`<div class="ne-form-section-head nea-head"><div><span class="ne-section-kicker">04 • VIDEO & AUDIO</span><strong>Lampiran multimedia posting</strong><small>Video/audio ikut terbit pada berita yang sama. Penyimpanan memakai Cloudinary Free, tanpa kartu dan tanpa GitHub token.</small></div><div class="nea-counts"><span class="nea-badge" id="nea-video-count">0/5 video</span><span class="nea-badge audio" id="nea-audio-count">0 audio</span></div></div><div id="nea-cloud-ready" class="nea-cloud-ready" hidden></div><div id="nea-cloud-setup" class="nea-cloud-setup"><strong>Aktifkan penyimpanan gratis sekali saja</strong><small>Gunakan akun Cloudinary Free. Cloud name dan unsigned upload preset bukan password dan boleh disimpan di browser.</small><div class="nea-cloud-fields"><label>Cloud name<input id="nea-cloud-name" value="${esc(getCloudName())}" placeholder="contoh: k2mss5sn"></label><label>Unsigned upload preset<input id="nea-upload-preset" value="${esc(getPreset())}" placeholder="contoh: paibp_media_free"></label></div><button id="nea-save-cloud" type="button">Simpan konfigurasi</button></div><div class="nea-upload-grid"><label class="nea-picker"><input id="nea-video" type="file" accept="video/*,.mp4,.webm,.mov,.m4v,.avi,.mkv,.3gp,.3g2,.ogv,.mpeg,.mpg,.mts,.m2ts,.wmv,.flv" multiple><span><b>＋ Unggah video</b><small>Maksimal 5 video per posting • maksimal 100 MB per file</small></span></label><label class="nea-picker audio"><input id="nea-audio" type="file" accept="audio/*,.mp3,.wav,.m4a,.aac,.ogg,.oga,.opus,.flac,.weba,.aiff,.aif,.amr,.wma" multiple><span><b>＋ Unggah MP3 / audio</b><small>Jumlah audio tidak dibatasi editor • maksimal 25 MB per file</small></span></label></div><div class="nea-list" id="nea-list"></div><div class="nea-progress" id="nea-progress"><span></span></div><div class="nea-status" id="nea-status" aria-live="polite"></div><p class="nea-note"><b>Ringan di HP:</b> media tidak dipreload pada halaman berita; file baru diminta ketika pengunjung menekan Putar.</p>`;actions?.before(sec);$("#nea-video").onchange=e=>{add("video",e.target.files||[]);e.target.value=""};$("#nea-audio").onchange=e=>{add("audio",e.target.files||[]);e.target.value=""};$("#nea-save-cloud").onclick=storeConfig;renderSetup();render()}
mount();
document.addEventListener("submit",e=>{if(e.target?.id!=="ne-form")return;if(bypassSubmit){bypassSubmit=false;return}if(!videoQueue.length&&!audioQueue.length&&!remote.length&&!dirty)return;e.preventDefault();e.stopImmediatePropagation();prepareAndContinue()},{capture:true});
document.addEventListener("click",e=>{const edit=e.target.closest?.("[data-edit]");if(edit)setTimeout(()=>loadRemote($("#ne-id")?.value||edit.dataset.edit),80);if(e.target.closest?.("#ne-reset")){videoQueue=[];audioQueue=[];remote=[];loadingId="";dirty=false;render();status("");progress(0)}},true);const initial=$("#ne-id")?.value;if(initial)loadRemote(initial);
})();
