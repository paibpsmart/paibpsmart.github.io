(() => {
  "use strict";
  const GAS="https://script.google.com/macros/s/AKfycbyRxOw6oWDZUuQxwuqOMRO92KOwqOGF_9J6rPzSfxr9Dqy9kAQGJ9qZA6Tm_deUOgtjKg/exec";
  const READ_KEY="b082937b2165453ba7d9f81ecac063b00310b339ec0643da";
  const DB_NAME="paibp-smart-news-editor-v96",STORE="posts",MAX_PHOTOS=10;
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
  const uid=()=>crypto?.randomUUID?.()||`news-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;
  const slug=s=>String(s||"").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,90)||`berita-${Date.now()}`;
  let photos=[],remote=[];

  function db(){return new Promise((resolve,reject)=>{const r=indexedDB.open(DB_NAME,1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:"id"})};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
  async function allLocal(){const d=await db();return new Promise((resolve,reject)=>{const tx=d.transaction(STORE,"readonly"),r=tx.objectStore(STORE).getAll();r.onsuccess=()=>resolve(r.result||[]);r.onerror=()=>reject(r.error)})}
  async function putLocal(item){const d=await db();return new Promise((resolve,reject)=>{const tx=d.transaction(STORE,"readwrite");tx.objectStore(STORE).put(item);tx.oncomplete=()=>resolve(item);tx.onerror=()=>reject(tx.error)})}
  async function delLocal(id){const d=await db();return new Promise((resolve,reject)=>{const tx=d.transaction(STORE,"readwrite");tx.objectStore(STORE).delete(id);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}

  function status(msg,tone=""){$("#ne-form-status").textContent=msg||"";$("#ne-form-status").dataset.tone=tone}
  function photoStatus(msg,tone=""){$("#ne-photo-info").textContent=msg||"";$("#ne-photo-info").dataset.tone=tone}
  function formatDate(v){if(!v)return"—";try{return new Intl.DateTimeFormat("id-ID",{day:"numeric",month:"long",year:"numeric"}).format(new Date(`${v}T12:00:00`))}catch{return v}}
  function sleep(ms){return new Promise(r=>setTimeout(r,ms))}

  function jsonp(action,params={}){return new Promise((resolve,reject)=>{const cb=`__ne101${Date.now()}${Math.random().toString(36).slice(2)}`,s=document.createElement("script");let done=false;const timer=setTimeout(()=>finish(new Error("Server berita belum merespons.")),10000);function finish(err,data){if(done)return;done=true;clearTimeout(timer);try{delete window[cb]}catch{};s.remove();err?reject(err):resolve(data)}window[cb]=data=>finish(null,data);const u=new URL(GAS);u.searchParams.set("action",action);u.searchParams.set("callback",cb);u.searchParams.set("_",Date.now());Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!==null)u.searchParams.set(k,String(v))});s.onerror=()=>finish(new Error("Koneksi ke server berita gagal."));s.src=u.href;document.head.append(s)})}
  async function snapshot(){const j=await jsonp("publicSnapshot");if(j?.ok!==true)throw new Error(j?.error||"Snapshot berita tidak tersedia.");return j}
  async function post(action,data){await fetch(GAS,{method:"POST",mode:"no-cors",cache:"no-store",headers:{"Content-Type":"text/plain;charset=UTF-8"},body:JSON.stringify({action,readKey:READ_KEY,key:READ_KEY,data:{...data,readKey:READ_KEY},origin:location.origin})});return true}
  async function waitFor(check,tries=7){let last=null;for(let i=0;i<tries;i++){await sleep(i?650+250*i:450);try{last=await snapshot();const value=check(last);if(value)return{snapshot:last,value}}catch{} }throw new Error("Server belum mengonfirmasi perubahan. Draft tetap aman.")}

  async function compress(file,maxW=1280,quality=.74){const bitmap=await createImageBitmap(file),scale=Math.min(1,maxW/bitmap.width),w=Math.max(1,Math.round(bitmap.width*scale)),h=Math.max(1,Math.round(bitmap.height*scale)),c=document.createElement("canvas");c.width=w;c.height=h;c.getContext("2d",{alpha:false}).drawImage(bitmap,0,0,w,h);bitmap.close?.();const blob=await new Promise(r=>c.toBlob(r,"image/webp",quality));return await new Promise((resolve,reject)=>{const fr=new FileReader();fr.onload=()=>resolve(fr.result);fr.onerror=()=>reject(fr.error);fr.readAsDataURL(blob)})}
  async function addFiles(files){const room=MAX_PHOTOS-photos.length;if(room<=0){photoStatus("Maksimal 10 foto untuk satu berita.","error");return}const selected=[...files].slice(0,room);if(files.length>room)photoStatus(`Maksimal 10 foto. ${selected.length} foto diproses.`,"error");for(let i=0;i<selected.length;i++){const f=selected[i];photoStatus(`Mengoptimalkan foto ${i+1} dari ${selected.length}…`);try{const full=await compress(f,1280,.74),thumb=await compress(f,480,.67);photos.push({id:uid(),name:f.name,full,thumb});renderPhotos();previewCard()}catch{photoStatus(`Foto ${f.name} gagal diproses.`,"error")}}photoStatus(`${photos.length} dari 10 foto siap. Foto pertama menjadi sampul.`)}

  function renderPhotos(){const root=$("#ne-media-grid"),count=$("#ne-photo-count");if(count)count.textContent=`${photos.length}/10 foto`;if(!photos.length){root.innerHTML='<div class="ne-media-empty"><div><strong>Belum ada foto</strong><br><small>Pilih hingga 10 foto. Foto pertama otomatis menjadi sampul.</small></div></div>';return}root.innerHTML=photos.map((p,i)=>`<article class="ne-photo-card ${i===0?'is-cover':''}"><span class="ne-photo-badge">${i===0?'SAMPUL':`FOTO ${i+1}`}</span><img src="${esc(p.thumb||p.full)}" alt="Pratinjau foto ${i+1}"><footer><span class="ne-photo-name">${esc(p.name||`Foto ${i+1}`)}</span><div class="ne-photo-actions"><button type="button" data-photo-left="${i}">←</button><button type="button" data-photo-right="${i}">→</button><button type="button" class="is-cover-btn" data-photo-cover="${i}">${i===0?'Sampul aktif':'Jadikan sampul'}</button><button type="button" class="is-danger" data-photo-remove="${i}">×</button></div></footer></article>`).join("");$$('[data-photo-left]',root).forEach(b=>b.onclick=()=>move(Number(b.dataset.photoLeft),-1));$$('[data-photo-right]',root).forEach(b=>b.onclick=()=>move(Number(b.dataset.photoRight),1));$$('[data-photo-cover]',root).forEach(b=>b.onclick=()=>cover(Number(b.dataset.photoCover)));$$('[data-photo-remove]',root).forEach(b=>b.onclick=()=>remove(Number(b.dataset.photoRemove))}
  function move(i,d){const n=i+d;if(n<0||n>=photos.length)return;[photos[i],photos[n]]=[photos[n],photos[i]];renderPhotos();previewCard()}
  function cover(i){if(i<=0||i>=photos.length)return;const[p]=photos.splice(i,1);photos.unshift(p);renderPhotos();previewCard()}
  function remove(i){photos.splice(i,1);renderPhotos();previewCard();photoStatus(`${photos.length} dari 10 foto siap.`)}
  $("#ne-photo").addEventListener("change",async e=>{await addFiles(e.target.files||[]);e.target.value=""});

  function previewCard(){const title=$("#ne-title").value.trim()||"Judul berita akan tampil di sini",summary=$("#ne-summary").value.trim()||"Ringkasan berita akan muncul otomatis ketika Anda mengetik.",cat=$("#ne-category").value,date=$("#ne-date").value;$("#ne-preview-title").textContent=title;$("#ne-preview-summary").textContent=summary;$("#ne-preview-category").textContent=cat;$("#ne-preview-date").textContent=formatDate(date);const media=$("#ne-preview-media");media.style.backgroundImage=photos[0]?.thumb?`url(${photos[0].thumb})`:"";media.innerHTML=photos[0]?.thumb?"":"<span>SPENSUS</span>";const mini=$("#ne-mini-gallery");mini.innerHTML=photos.slice(1,5).map(p=>`<img src="${esc(p.thumb||p.full)}" alt="Foto tambahan">`).join("");mini.hidden=photos.length<2}
  ["#ne-title","#ne-summary","#ne-category","#ne-date","#ne-content"].forEach(s=>$(s).addEventListener("input",previewCard));

  function formItem(state="draft",requirePhoto=true){const id=$("#ne-id").value||uid(),title=$("#ne-title").value.trim(),summary=$("#ne-summary").value.trim(),content=$("#ne-content").value.trim(),date=$("#ne-date").value,category=$("#ne-category").value;if(!title||!summary||!content||!date)throw new Error("Judul, tanggal, ringkasan, dan isi berita wajib diisi.");if(requirePhoto&&!photos.length)throw new Error("Tambahkan minimal satu foto.");const now=new Date().toISOString();return{id,slug:slug(title),title,summary,content,date,category,year:Number(date.slice(0,4)),month:Number(date.slice(5,7)),thumbnail:photos[0]?.thumb||photos[0]?.full||"",images:photos.map(p=>p.full),media:photos.map((p,i)=>({src:p.full,thumbnail:p.thumb||p.full,name:p.name,order:i+1,cover:i===0})),status:state,author:"Sunarso, S.Pd.I, Gr",updatedAt:now,createdAt:now}}
  async function saveDraft(){try{const item=formItem("draft",false),old=(await allLocal()).find(x=>x.id===item.id);if(old)item.createdAt=old.createdAt;await putLocal(item);$("#ne-id").value=item.id;$("#ne-editor-status").textContent="Draft tersimpan";status("Draft tersimpan di perangkat. Tulisan Anda aman.","ok");refreshLists()}catch(e){status(e.message||"Draft gagal disimpan.","error")}}
  $("#ne-save-draft").addEventListener("click",saveDraft);

  async function uploadPhotoUrl(articleId,index,item,photo){const tempId=`media-${articleId}-${index}-${Date.now().toString(36)}`;await post("newsUpsert",{id:tempId,title:"__MEDIA__",date:item.date,summary:"media",imageDataUrl:photo.full,authorName:item.author,authorSchool:"SMP Negeri 1 Susukan",isPublished:true,sortOrder:-999999});const found=await waitFor(s=>{const row=(s.news||[]).find(n=>String(n.id)===tempId);return row?.imageUrl?row:null});const url=found.value.imageUrl;post("newsDelete",{id:tempId}).catch(()=>{});return url}

  async function publishDirect(){let item;try{item=formItem("pending",true)}catch(e){status(e.message,"error");return}const button=$("#ne-publish-direct");button.disabled=true;let old;try{old=(await allLocal()).find(x=>x.id===item.id);if(old)item.createdAt=old.createdAt;await putLocal(item);const urls=[];for(let i=0;i<photos.length;i++){button.textContent=`Foto ${i+1}/${photos.length}`;status(`Mengunggah dan memverifikasi foto ${i+1} dari ${photos.length}…`);urls.push(await uploadPhotoUrl(item.id,i,item,photos[i]))}button.textContent="Menyimpan…";status("Menyimpan naskah lengkap dan galeri…");const contentValue={id:item.id,slug:item.slug,title:item.title,date:item.date,category:item.category,summary:item.summary,content:item.content,media:urls,coverUrl:urls[0],year:item.year,month:item.month,author:item.author};const encoded=JSON.stringify(contentValue);if(encoded.length>43000)throw new Error("Naskah terlalu panjang untuk satu posting. Ringkas isi berita sedikit lalu coba lagi.");await post("contentUpsert",{key:`news:${item.id}`,value:contentValue,authorName:item.author,authorSchool:"SMP Negeri 1 Susukan"});await post("newsUpsert",{id:item.id,title:item.title,date:item.date,summary:item.summary,imageUrl:urls[0],authorName:item.author,authorSchool:"SMP Negeri 1 Susukan",isPublished:true,sortOrder:Date.now()});button.textContent="Verifikasi…";status("Memastikan berita benar-benar sudah tayang…");const verified=await waitFor(s=>{const row=(s.news||[]).find(n=>String(n.id)===String(item.id));const extra=s.content?.[`news:${item.id}`];return row&&extra?{row,extra}:null},8);item={...item,status:"published",publishedAt:verified.value.row.updatedAt||new Date().toISOString(),thumbnail:verified.value.extra.coverUrl||urls[0],images:urls,media:urls.map((url,i)=>({src:url,thumbnail:url,name:photos[i]?.name||`Foto ${i+1}`,order:i+1,cover:i===0}))};await putLocal(item);$("#ne-id").value=item.id;$("#ne-editor-status").textContent="Tayang";status(`Berhasil. Berita sudah tayang dan terverifikasi dengan ${urls.length} foto.`,"ok");remote=normalizeSnapshot(verified.snapshot);refreshLists();document.dispatchEvent(new CustomEvent("paibp:editor-news-published",{detail:{id:item.id}}))}catch(e){item.status="draft";await putLocal(item);$("#ne-editor-status").textContent="Draft";status(`Belum tayang: ${e.message||"server belum mengonfirmasi"} Tulisan tetap aman sebagai Draft.`,"error")}finally{button.disabled=false;button.textContent="Terbitkan"}}
  $("#ne-form").addEventListener("submit",e=>{e.preventDefault();publishDirect()});

  function resetForm(){$("#ne-form").reset();$("#ne-id").value="";$("#ne-date").value=new Date().toISOString().slice(0,10);photos=[];renderPhotos();photoStatus("Belum ada foto dipilih. Maksimal 10 foto.");$("#ne-editor-status").textContent="Draft baru";status("");previewCard()}
  $("#ne-reset").addEventListener("click",resetForm);
  function normalizeMedia(x){if(Array.isArray(x?.media)&&x.media.length)return x.media.slice(0,10).map((m,i)=>({id:uid(),name:m.name||`Foto ${i+1}`,full:m.src||m.full||"",thumb:m.thumbnail||m.thumb||m.src||""})).filter(p=>p.full||p.thumb);if(Array.isArray(x?.images))return x.images.slice(0,10).map((src,i)=>({id:uid(),name:`Foto ${i+1}`,full:src,thumb:src}));if(x?.thumbnail)return[{id:uid(),name:"Foto sampul",full:x.thumbnail,thumb:x.thumbnail}];return[]}
  function normalizeSnapshot(s){const content=s?.content||{};return (s?.news||[]).filter(n=>n?.id&&!/^media-/i.test(String(n.id))&&!/^__MEDIA__/i.test(String(n.title||""))).map(n=>{const e=content[`news:${n.id}`]||{},urls=Array.isArray(e.media)?e.media.filter(Boolean):(n.imageUrl?[n.imageUrl]:[]),date=e.date||n.date||"";return{id:String(n.id),title:e.title||n.title||"",summary:e.summary||n.summary||"",content:e.content||n.summary||"",date,category:e.category||"Berita Sekolah",year:Number(e.year||String(date).slice(0,4))||0,month:Number(e.month||String(date).slice(5,7))||0,thumbnail:e.coverUrl||n.imageUrl||urls[0]||"",images:urls,media:urls.map((url,i)=>({src:url,thumbnail:url,name:`Foto ${i+1}`,order:i+1,cover:i===0})),status:"published",publishedAt:n.updatedAt||date,updatedAt:n.updatedAt||date,author:n.authorName||"Sunarso, S.Pd.I, Gr"}})}
  function merge(local){const m=new Map();[...remote,...local].forEach(x=>x?.id&&m.set(x.id,{...m.get(x.id),...x}));return[...m.values()].sort((a,b)=>String(b.publishedAt||b.updatedAt||b.date).localeCompare(String(a.publishedAt||a.updatedAt||a.date)))}
  async function refreshRemote(){try{remote=normalizeSnapshot(await snapshot())}catch{}return remote}
  async function refreshLists(){const items=merge(await allLocal());renderFilters(items);renderPosts(items);renderArchive(items)}
  function renderFilters(items){const years=[...new Set(items.map(x=>x.year).filter(Boolean))].sort((a,b)=>b-a),cats=[...new Set(items.map(x=>x.category).filter(Boolean))].sort(),y=$("#ne-filter-year"),c=$("#ne-filter-category"),yv=y.value,cv=c.value;y.innerHTML='<option value="">Semua tahun</option>'+years.map(v=>`<option>${v}</option>`).join("");c.innerHTML='<option value="">Semua kategori</option>'+cats.map(v=>`<option>${esc(v)}</option>`).join("");y.value=yv;c.value=cv}
  function filtered(items){const q=$("#ne-search").value.toLowerCase().trim(),y=$("#ne-filter-year").value,c=$("#ne-filter-category").value;return items.filter(x=>(!q||String(x.title).toLowerCase().includes(q))&&(!y||String(x.year)===y)&&(!c||x.category===c))}
  function renderPosts(items){const list=$("#ne-post-list"),rows=filtered(items);if(!rows.length){list.innerHTML='<div class="ne-empty">Belum ada posting yang sesuai.</div>';return}list.innerHTML=rows.map(x=>`<article class="ne-post"><div class="ne-post-thumb"${x.thumbnail?` style="background-image:url('${esc(x.thumbnail)}')"`:""}></div><div><h3>${esc(x.title)}</h3><p>${esc(x.summary||"")}</p><div class="ne-post-meta"><span>${esc(x.category||"")}</span><span>${esc(formatDate(x.date))}</span><span>${x.status==="published"?"Tayang":x.status==="pending"?"Mengirim":"Draft"}</span><span>${Array.isArray(x.images)?x.images.length:0} foto</span></div></div><div class="ne-post-actions"><button class="ne-edit" data-edit="${esc(x.id)}">Edit</button><button class="ne-delete" data-delete="${esc(x.id)}">Hapus</button></div></article>`).join("");$$('[data-edit]',list).forEach(b=>b.onclick=()=>editItem(items.find(x=>x.id===b.dataset.edit)));$$('[data-delete]',list).forEach(b=>b.onclick=()=>deleteItem(items.find(x=>x.id===b.dataset.delete))}
  function renderArchive(items){const root=$("#ne-archive"),years=[...new Set(items.map(x=>x.year).filter(Boolean))].sort((a,b)=>b-a);root.innerHTML=years.length?years.map(y=>{const a=items.filter(x=>x.year===y),p=a.filter(x=>x.status==="published").length;return`<article class="ne-year"><strong>${y}</strong><span>${a.length} berita/draft</span><b>${p} tayang</b></article>`}).join(""):'<div class="ne-empty">Arsip akan terbentuk otomatis setelah ada berita.</div>'}
  function switchTab(name){$$('[data-ne-tab]').forEach(b=>b.classList.toggle('is-active',b.dataset.neTab===name));$$('[data-ne-panel]').forEach(p=>p.classList.toggle('is-active',p.dataset.nePanel===name));if(name!=="write")refreshLists()}
  $$('[data-ne-tab]').forEach(b=>b.addEventListener('click',()=>switchTab(b.dataset.neTab)));
  async function editItem(x){if(!x)return;$("#ne-id").value=x.id;$("#ne-title").value=x.title||"";$("#ne-date").value=x.date||new Date().toISOString().slice(0,10);$("#ne-category").value=x.category||"Berita Sekolah";$("#ne-summary").value=x.summary||"";$("#ne-content").value=x.content||"";photos=normalizeMedia(x);renderPhotos();photoStatus(`${photos.length} dari 10 foto dimuat.`);$("#ne-editor-status").textContent=x.status==="published"?"Tayang":"Draft";previewCard();switchTab("write");scrollTo({top:0,behavior:"smooth"})}
  async function deleteItem(x){if(!x||!confirm(`Hapus “${x.title}”?`))return;if(x.status==="published"){await post("newsDelete",{id:x.id});try{await waitFor(s=>!(s.news||[]).some(n=>String(n.id)===String(x.id)),5)}catch{alert("Server belum mengonfirmasi penghapusan. Coba lagi beberapa saat.");return}}await delLocal(x.id);remote=remote.filter(r=>r.id!==x.id);refreshLists()}
  $("#ne-refresh").addEventListener("click",async()=>{await refreshRemote();refreshLists()});["#ne-search","#ne-filter-year","#ne-filter-category"].forEach(s=>$(s).addEventListener("input",refreshLists));

  $("#ne-date").value=new Date().toISOString().slice(0,10);renderPhotos();previewCard();refreshRemote().finally(refreshLists);
})();