(()=>{"use strict";if(window.__SHARE_READY_V152__)return;window.__SHARE_READY_V152__=1;const $=(s,r=document)=>r.querySelector(s);async function ready(url){try{const r=await fetch(url+(url.includes('?')?'&':'?')+'_ready='+Date.now(),{method:'HEAD',cache:'no-store',redirect:'follow'});return r.ok}catch{return false}}async function waitReady(url,btn){const old=btn.innerHTML;btn.disabled=true;btn.innerHTML='<b>…</b><span>Menyiapkan preview…</span>';for(let i=0;i<20;i++){if(await ready(url)){btn.disabled=false;btn.innerHTML=old;return true}await new Promise(r=>setTimeout(r,1500))}btn.disabled=false;btn.innerHTML=old;alert('Preview berita belum siap. Sistem masih menyiapkannya otomatis; coba Bagikan lagi beberapa saat lagi agar tautan tidak terkirim tanpa preview.');return false}document.addEventListener('click',async e=>{const btn=e.target.closest?.('#ig-share-modal-v146 [data-external="0"]');if(!btn)return;const input=$('#ig-share-link');const url=input?.value?.trim();if(!url)return;e.preventDefault();e.stopImmediatePropagation();if(!(await waitReady(url,btn)))return;const title=$('#ig-share-sub')?.textContent?.trim()||'Berita SMP Negeri 1 Susukan';window.open('https://wa.me/?text='+encodeURIComponent(title+'\n'+url),'_blank','noopener,noreferrer')},true)})();
