(() => {
  "use strict";
  if(window.__SPENSUS_ARTICLE_LIVE_V102__)return;window.__SPENSUS_ARTICLE_LIVE_V102__=true;
  const API="https://api.openalex.org/works",PAGE=30,$=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
  const topics=["Pendidikan","Remaja","Akhlak","Literasi Digital","Kecerdasan Artifisial","Sejarah","Lingkungan","Kesehatan","Sosial","Agama dan Pendidikan"];
  let state={query:"",page:1,count:0,results:[],busy:false,active:null};
  function abstractText(inv){if(!inv||typeof inv!=="object")return"";let max=0;Object.values(inv).forEach(a=>(a||[]).forEach(n=>{if(n>max)max=n}));const words=new Array(max+1);Object.entries(inv).forEach(([w,arr])=>(arr||[]).forEach(n=>{words[n]=w}));return words.filter(Boolean).join(" ")}
  function authors(w){return(w.authorships||[]).slice(0,4).map(a=>a?.author?.display_name).filter(Boolean).join(", ")||"Penulis tidak tercantum"}
  function pdfUrl(w){return w?.best_oa_location?.pdf_url||w?.primary_location?.pdf_url||""}
  function sourceName(w){return w?.primary_location?.source?.display_name||w?.best_oa_location?.source?.display_name||"Artikel"}
  function compactCount(n){if(!Number.isFinite(n))return"—";return new Intl.NumberFormat("id-ID",{notation:n>9999?"compact":"standard",maximumFractionDigits:1}).format(n)}

  function shell(){let s=$("#rh-live-catalog");if(s)return s;const tools=$(".article-tools-v37");if(!tools)return null;s=document.createElement("section");s.id="rh-live-catalog";s.className="rh-live-catalog";s.innerHTML=`<div class="rh-live-head"><h2>Katalog Artikel</h2></div><form class="rh-live-search"><input type="search" autocomplete="off" placeholder="Cari topik, judul, atau kata kunci…"><button type="submit">Cari</button></form><div class="rh-topic-row">${topics.map(t=>`<button type="button" data-rh-topic="${esc(t)}">${esc(t)}</button>`).join("")}</div><p class="rh-live-status"></p><div class="rh-live-grid"></div><button class="rh-live-more" type="button" hidden>Tampilkan lebih banyak</button></section>`;tools.parentElement.insertBefore(s,$("#article-grid")||tools.nextSibling);bind(s);return s}
  function modal(){let m=$("#rh-preview");if(m)return m;m=document.createElement("div");m.id="rh-preview";m.className="rh-preview";m.hidden=true;m.innerHTML=`<section class="rh-preview-card" role="dialog" aria-modal="true" aria-labelledby="rh-preview-title"><div class="rh-preview-top"><strong>Preview</strong><button type="button" data-rh-close aria-label="Tutup">×</button></div><div class="rh-preview-body"></div></section>`;document.body.appendChild(m);m.addEventListener("click",e=>{if(e.target===m||e.target.closest("[data-rh-close]"))closePreview()});return m}
  function closePreview(){const m=modal();m.hidden=true;document.body.style.overflow="";state.active=null}
  document.addEventListener("keydown",e=>{if(e.key==="Escape"&&!modal().hidden)closePreview()});
  function bind(s){const form=$(".rh-live-search",s),input=$("input",form),more=$(".rh-live-more",s);form.addEventListener("submit",e=>{e.preventDefault();const q=input.value.trim();if(q)search(q,1)});$$('[data-rh-topic]',s).forEach(b=>b.addEventListener('click',()=>{input.value=b.dataset.rhTopic;search(b.dataset.rhTopic,1)}));more.addEventListener('click',()=>search(state.query,state.page+1,true));$(".rh-live-grid",s).addEventListener("click",e=>{const b=e.target.closest("[data-rh-preview]");if(b)openPreview(b.dataset.rhPreview)});}

  async function search(q,page=1,append=false){if(state.busy)return;state.busy=true;state.query=q;const s=shell(),status=$(".rh-live-status",s),grid=$(".rh-live-grid",s),more=$(".rh-live-more",s);status.textContent="Memuat…";if(!append)grid.innerHTML='<div class="article-empty-v37"><span>⌕</span><h2>Memuat katalog…</h2></div>';more.hidden=true;const ctl=new AbortController(),tm=setTimeout(()=>ctl.abort(),9000);try{const u=new URL(API);u.searchParams.set("search",q);u.searchParams.set("filter","open_access.is_oa:true");u.searchParams.set("per-page",PAGE);u.searchParams.set("page",page);const r=await fetch(u,{cache:"no-store",headers:{Accept:"application/json"},signal:ctl.signal});if(!r.ok)throw new Error(`HTTP ${r.status}`);const j=await r.json(),raw=Array.isArray(j.results)?j.results:[],list=raw.filter(w=>pdfUrl(w)||abstractText(w.abstract_inverted_index).length>160);state.count=Number(j?.meta?.count||0);state.page=page;state.results=append?[...state.results,...list]:list;renderResults();status.textContent=`${compactCount(state.count)} hasil`;more.hidden=raw.length<PAGE}catch(e){if(!append)grid.innerHTML='<div class="article-empty-v37"><span>↻</span><h2>Belum tersambung</h2></div>';status.textContent=""}finally{clearTimeout(tm);state.busy=false}}

  function renderResults(){const s=shell(),grid=$(".rh-live-grid",s);grid.innerHTML=state.results.map((w,i)=>{const title=w.display_name||w.title||"Tanpa judul",year=w.publication_year||"—";return`<article class="rh-work"><div class="rh-work-cover"><span>${esc(sourceName(w))} • ${esc(year)}</span><b>${esc(title)}</b></div><div class="rh-work-body"><strong>${esc(title)}</strong><small>${esc(authors(w))}</small><div class="rh-work-actions"><button type="button" data-rh-preview="${esc(w.id)}">Preview</button></div></div></article>`}).join("")||'<div class="article-empty-v37"><span>⌕</span><h2>Belum ditemukan</h2></div>'}

  function openPreview(id){const w=state.results.find(x=>x.id===id);if(!w)return;state.active=w;const m=modal(),body=$(".rh-preview-body",m),title=w.display_name||w.title||"Tanpa judul",abs=abstractText(w.abstract_inverted_index),pdf=pdfUrl(w);body.innerHTML=`<h2 id="rh-preview-title">${esc(title)}</h2><div class="rh-preview-meta"><span>${esc(w.publication_year||"—")}</span><span>${esc(sourceName(w))}</span><span>${esc(w.type||"article")}</span></div><p><strong>${esc(authors(w))}</strong></p>${abs?`<p>${esc(abs)}</p>`:""}${pdf?`<div class="rq-preview-actions-clean"><button type="button" data-rh-inline-pdf>Baca PDF di sini</button><a class="is-download" href="${esc(pdf)}" download>Unduh PDF</a></div>`:""}`;const b=$("[data-rh-inline-pdf]",body);if(b)b.onclick=()=>{let r=$(".rq-inline-reader",body);if(!r){r=document.createElement("div");r.className="rq-inline-reader";r.innerHTML=`<iframe src="${esc(pdf)}#toolbar=1&navpanes=0" title="Artikel PDF"></iframe>`;body.appendChild(r)}r.scrollIntoView({behavior:"smooth",block:"start"})};m.hidden=false;document.body.style.overflow="hidden"}

  function init(){shell();modal();const count=$("#article-count");if(count)count.textContent="";}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();